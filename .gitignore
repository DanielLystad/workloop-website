# ── macOS ─────────────────────────────────────────────────────────
.DS_Store
.AppleDouble
.LSOverride
Icon

# ── Windows ───────────────────────────────────────────────────────
Thumbs.db
ehthumbs.db
Desktop.ini

# ── Pinegrow editor artifacts ─────────────────────────────────────
# These are generated by the Pinegrow visual editor and have no
# place in version control.
pinegrow.json
_pgbackup/
_pginfo/
pgia/

# ── Node / npm (future-proofing) ──────────────────────────────────
node_modules/
npm-debug.log*
package-lock.json

# ── Environment & secrets ─────────────────────────────────────────
.env
.env.local
.env.*.local

# ── Editor metadata ───────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
